import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.gps_demo',
  appName: 'gps_tutorial',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
